# 🧠 **What is etcd in Kubernetes?**

* ***etcd** is a **distributed, consistent key-value database** used by Kubernetes to store **all cluster state**.
* It is the **single source of truth** for everything in the cluster.
* If Kubernetes were a brain, **etcd is the memory**.

Store and replicate the entire Kubernetes cluster state in a consistent and highly available way.